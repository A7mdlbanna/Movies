import 'package:flutter/material.dart';

class ActorInfoScreen extends StatelessWidget {
  const ActorInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
